# ==========================================
# Настройка для оффлайн развертывания
# ==========================================

# Аутентификация (ОБЯЗАТЕЛЬНО)
# Сгенерируйте случайный секрет: openssl rand -base64 32
AUTH_SECRET=your-generated-secret-here

# ==========================================
# OpenAI-совместимый API
# ==========================================

# Базовый URL для вашего AI API
# Примеры:
# - OpenAI: https://api.openai.com/v1
# - Ollama (локально): http://localhost:11434/v1
# - LM Studio (локально): http://localhost:1234/v1
# - vLLM: http://your-server:8000/v1
# - Any OpenAI-compatible API
OPENAI_BASE_URL=http://localhost:11434/v1

# API ключ (может быть пустым для локальных API)
# Для OpenAI: ваш реальный ключ
# Для локальных (Ollama, LM Studio): любая непустая строка, например "local"
OPENAI_API_KEY=local

# Названия моделей (отображаются в UI)
CHAT_MODEL_TITLE="Qwen3 32B"
REASONING_MODEL_TITLE="Qwen3 32B Thinking"

# Модели для использования (настройте под ваши доступные модели)
# Для Ollama используйте имена моделей типа: llama3.2, mistral, qwen2.5, etc.
# Для OpenAI: gpt-4o, gpt-4o-mini, etc.
CHAT_MODEL=llama3.2
REASONING_MODEL=llama3.2
TITLE_MODEL=llama3.2
ARTIFACT_MODEL=llama3.2

# ==========================================
# База данных (ОБЯЗАТЕЛЬНО)
# ==========================================

# PostgreSQL connection string
# Формат: postgresql://username:password@host:port/database
POSTGRES_URL=postgresql://user:password@localhost:5432/chatbot

# ==========================================
# Хранилище файлов - MinIO (ОПЦИОНАЛЬНО)
# ==========================================

# MinIO - S3-совместимое объектное хранилище для загрузки файлов
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=chatbot-uploads

# ==========================================
# Redis (ОПЦИОНАЛЬНО - для rate limiting)
# ==========================================

# Если не используете Redis для rate limiting - закомментируйте
# REDIS_URL=redis://localhost:6379

# ==========================================
# Примеры конфигурации для разных AI провайдеров
# ==========================================

# === Ollama (локально) ===
# OPENAI_BASE_URL=http://localhost:11434/v1
# OPENAI_API_KEY=local
# CHAT_MODEL=llama3.2
# REASONING_MODEL=qwen2.5
# TITLE_MODEL=llama3.2
# ARTIFACT_MODEL=llama3.2

# === LM Studio (локально) ===
# OPENAI_BASE_URL=http://localhost:1234/v1
# OPENAI_API_KEY=local
# CHAT_MODEL=your-loaded-model-name

# === vLLM ===
# OPENAI_BASE_URL=http://your-vllm-server:8000/v1
# OPENAI_API_KEY=your-api-key
# CHAT_MODEL=your-model-name

# === OpenAI ===
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=sk-your-actual-openai-key
# CHAT_MODEL=gpt-4o
# REASONING_MODEL=gpt-4o
# TITLE_MODEL=gpt-4o-mini
# ARTIFACT_MODEL=gpt-4o

# ==========================================
# SSL Certificate Settings
# ==========================================

# Если используете самоподписанные сертификаты (например, в корпоративной сети)
# ВНИМАНИЕ: Используйте только в development или доверенных внутренних сетях!
# НЕ используйте в production с внешними API!
# NODE_TLS_REJECT_UNAUTHORIZED=0
